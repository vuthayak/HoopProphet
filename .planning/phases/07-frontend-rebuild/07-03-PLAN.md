---
phase: 07-frontend-rebuild
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - hoopprophet/src/components/PropCard.jsx
  - hoopprophet/src/components/LineSlider.jsx
  - hoopprophet/src/components/HitRateChart.jsx
  - hoopprophet/src/components/GameLogTable.jsx
  - hoopprophet/src/components/NewsList.jsx
  - hoopprophet/src/components/PlayerHeader.jsx
  - hoopprophet/src/pages/HomePage.jsx
  - hoopprophet/src/pages/PlayerPage.jsx
  - hoopprophet/src/__tests__/HitRateChart.test.jsx
  - hoopprophet/src/__tests__/PropCard.test.jsx
  - hoopprophet/src/__tests__/GameLogTable.test.jsx
autonomous: true
requirements:
  - UI-03
  - UI-04
  - UI-05
  - UI-08
  - PROP-02
  - PROP-03
  - NEWS-03
  - D-06
  - D-07
  - D-08
  - D-10
  - D-11
  - D-13
  - D-14
  - D-16
  - D-18

must_haves:
  truths:
    - "Player page shows prop cards with probability badges and hit rate charts"
    - "Adjusting line slider fetches updated hit rates with 300ms debounce"
    - "Hit rate chart renders 4 bars for L5/L10/L20/Season windows"
    - "Game log table shows recent game stats in a compact scrollable table"
    - "News/injury alerts appear as colored badges next to player name"
    - "Home page displays search prompt with prominent search input"
    - "Player page uses tabbed layout: Overview / Game Logs / News"
  artifacts:
    - path: "hoopprophet/src/components/PropCard.jsx"
      provides: "Prop card with probability badge, line slider, hit rate chart"
      contains: "ProbabilityBadge"
    - path: "hoopprophet/src/components/HitRateChart.jsx"
      provides: "Visx bar chart for L5/L10/L20/Season hit rates"
      contains: "ParentSize"
    - path: "hoopprophet/src/components/LineSlider.jsx"
      provides: "Half-point adjustable line slider"
      contains: "step"
    - path: "hoopprophet/src/components/GameLogTable.jsx"
      provides: "Compact scrollable game log table"
      contains: "overflow"
    - path: "hoopprophet/src/pages/PlayerPage.jsx"
      provides: "Tabbed player analysis page"
      contains: "TabBar"
    - path: "hoopprophet/src/pages/HomePage.jsx"
      provides: "Search landing page with empty state"
      contains: "Search for a player"
  key_links:
    - from: "hoopprophet/src/components/PropCard.jsx"
      to: "hoopprophet/src/hooks/useHitRates.js"
      via: "debounced hit rate refetch on line change"
      pattern: "useHitRates"
    - from: "hoopprophet/src/pages/PlayerPage.jsx"
      to: "hoopprophet/src/hooks/usePlayerData.js"
      via: "Promise.all parallel data fetch"
      pattern: "usePlayerData"
    - from: "hoopprophet/src/components/HitRateChart.jsx"
      to: "@visx/* packages"
      via: "Visx BarGroup rendering"
      pattern: "@visx"

---

<objective>
Build the core feature components that make up the player analysis experience: PropCard with probability badge and hit rate visualization, LineSlider for adjustable lines, GameLogTable, NewsList, PlayerHeader, and the HomePage and PlayerPage layouts. This is the heart of the application.

Purpose: Delivers the primary user value — viewing player prop predictions with ML probability, hit rate charts, adjustable lines, game logs, and news alerts.
Output: Fully functional player pages with prop cards, charts, and search landing page.
</objective>

<execution_context>
@/Users/vishvah/Documents/GitHub/HoopProphet/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/vishvah/Documents/GitHub/HoopProphet/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-frontend-rebuild/07-CONTEXT.md
@.planning/phases/07-frontend-rebuild/07-RESEARCH.md
@.planning/phases/07-frontend-rebuild/07-UI-SPEC.md
@.planning/phases/07-frontend-rebuild/07-02-SUMMARY.md

<interfaces>
<!-- From Plan 02: Hooks and primitive components this plan uses -->

From hoopprophet/src/hooks/usePlayerData.js:
```js
export function usePlayerData(playerId) {
  // Returns { player, props, lines, alerts, loading, error }
  // Parallel fetch via Promise.all
}
```

From hoopprophet/src/hooks/useHitRates.js:
```js
export function useHitRates(playerId, stat, line) {
  // Returns { hitRates, loading, error }
  // 300ms debounce on stat/line changes
}
```

From hoopprophet/src/hooks/useGameLogs.js:
```js
export function useGameLogs(playerId, limit = 50) {
  // Returns { gamelogs, loading, error }
}
```

From hoopprophet/src/hooks/useNews.js:
```js
export function useNews(playerId) {
  // Returns { news, alerts, staleWarning, loading, error }
}
```

From hoopprophet/src/components/ProbabilityBadge.jsx:
```jsx
// Props: probability (number 0-1)
// Renders: colored percentage badge (green/yellow/red per thresholds)
```

From hoopprophet/src/components/AlertBadge.jsx:
```jsx
// Props: alert (object with alert_type, headline)
// Renders: colored pill badge
```

From hoopprophet/src/components/TabBar.jsx:
```jsx
// Props: tabs (array of {id, label}), activeTab (string), onTabChange (function)
// Renders: horizontal tab bar with green underline on active
```

From hoopprophet/src/components/Navbar.jsx:
```jsx
// Renders: fixed top nav bar with logo, PlayerSearch, Backtest link
```

From hoopprophet/src/components/skeleton/SkeletonCard.jsx:
```jsx
// Renders: pulsing gray rectangle matching PropCard dimensions
```

From hoopprophet/src/api/client.js:
```js
export const api = {
  getPlayerHitRates: (id, stat, line) => fetchJSON(`/api/players/${id}/hitrates?stat=${stat}&line=${line}`),
  // ... other methods
};
```

<!-- Visx v4 Alpha API (from @next tag) -->
From @visx/shape: Bar component for bar chart rendering
From @visx/group: Group component for chart element positioning
From @visx/scale: scaleBand, scaleLinear for bar chart axes
From @visx/axis: AxisBottom, AxisLeft for chart axis rendering
From @visx/responsive: ParentSize for responsive chart containers
From @visx/gradient: Gradient for bar chart fill effects
From @visx/tooltip: Tooltip for bar hover interactions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PropCard, HitRateChart, LineSlider, and GameLogTable components</name>
  <files>
    hoopprophet/src/components/PropCard.jsx, hoopprophet/src/components/HitRateChart.jsx,
    hoopprophet/src/components/LineSlider.jsx, hoopprophet/src/components/GameLogTable.jsx,
    hoopprophet/src/__tests__/HitRateChart.test.jsx, hoopprophet/src/__tests__/PropCard.test.jsx,
    hoopprophet/src/__tests__/GameLogTable.test.jsx
  </files>
  <action>
    Create the 4 core data-display components that form the player analysis experience.

    1. **HitRateChart.jsx** — Per D-06, D-08, UI-08, and UI-SPEC Data Visualization:
       - Uses Visx (@visx/shape, @visx/group, @visx/scale, @visx/axis, @visx/responsive, @visx/tooltip)
       - IMPORTANT: All Visx imports use v4 alpha API. Import from '@visx/shape', '@visx/group', etc.
       - Wraps chart in `<ParentSize>` for responsive sizing per Pitfall 4 (NEVER use fixed width/height)
       - Renders 4 bars: L5, L10, L20, Season (always that order per UI-SPEC)
       - Conditional bar colors: green (#22c55e) if rate >= 55%, yellow (#eab308) if 40-55%, red (#ef4444) if < 40%
       - Chart dimensions: ~240px wide × 160px tall inside PropCard
       - Y-axis: 0-100% with gridlines at 25% increments (dashed, slate-700)
       - X-axis: window labels in label font (12px, medium, slate-400)
       - Null handling: windows with insufficient data (<5 games) show "—" instead of bar
       - Tooltip on bar hover: "{rate}% ({count} games)" in dark tooltip per UI-SPEC
       - Animation: bars grow from baseline 200ms ease-out on mount, re-animate on hit rate data change
       - Chart area padding: 16px around, 24px below for labels
       - Implementation follows RESEARCH.md Pattern 4 (Visx Hit Rate Bar Chart example)
       - Use the exact code pattern from 07-RESEARCH.md Pattern 4, adapted for Visx v4 alpha imports

    2. **LineSlider.jsx** — Per D-08, PROP-03, and UI-SPEC Interaction Pattern 3:
       - HTML range input with step="0.5" for half-point increments (sportsbook standard per D-08)
       - Range: default line value ± 10.0 (e.g., default 24.5 → range 14.5 to 34.5)
       - Display current line value below slider thumb: "24.5"
       - Visual: accent track fill (blue-500), dark track (slate-700)
       - 20px × 20px white slider thumb with shadow
       - Stat name displayed above slider (e.g., "Points")
       - aria-label="Adjust {stat} line" for accessibility
       - On slider release, calls onLineChange callback with new line value
       - Parent component (PropCard) handles debounced hit rate refetch via useHitRates
       - Mobile: 44px touch target per UI-SPEC spacing exceptions

    3. **PropCard.jsx** — Per UI-03, D-06, D-07, D-08, D-10, and UI-SPEC PropCard wireframe:
       - Accepts prop data: { stat, line, probability, direction, hit_rates }
       - Layout: compact card with bg-bg-card, rounded-lg, border border-border
       - Content structure:
         a. Stat name and line value header (e.g., "Points 24.5")
         b. ProbabilityBadge large centered display with "OVER" label below
         c. LineSlider for adjusting line value
         d. HitRateChart below slider showing hit rate bars
       - Hover effect: transition-all duration-150 hover:scale-[1.01] hover:border-slate-600
       - Responsive: min-w-[320px] per UI-SPEC spacing exceptions
       - When slider changes line value, PropCard:
         - Updates local line state
         - Calls useHitRates(playerId, stat, newLine) with 300ms debounce
         - Passes updated hitRates to HitRateChart
       - fadeSlideUp animation on mount (300ms per UI-SPEC)
       - Shows SkeletonCard during loading state
       - Card grid: 2-3 per row on desktop (lg:grid-cols-3), 2 on tablet (md:grid-cols-2), 1 on mobile

    4. **GameLogTable.jsx** — Per D-11, UI-04, and UI-SPEC:
       - Compact scrollable table of recent game stats
       - Columns: Date, Matchup, W/L, PTS, REB, AST, STL, BLK, MIN
       - Is DNP rows shown with text-text-muted styling and "DNP" indicator
       - Horizontal scroll on mobile (overflow-x-auto, no column hiding per UI-SPEC responsive details)
       - max-h-96 max height with scroll on overflow
       - Header row with text-text-secondary, sticky top
       - Data rows with hover:bg-bg-card-hover transition
       - Uses text-xs (label font) for stat values to maintain density
       - Empty state: "No game logs available for this player." per Copywriting Contract

    Create tests:
    - HitRateChart.test.jsx: renders 4 bars for L5/L10/L20/Season data, shows "—" for null windows
    - PropCard.test.jsx: renders stat name, probability badge, line slider; slider change triggers callback
    - GameLogTable.test.jsx: renders table rows from data, handles empty state, shows DNP indicator
  </action>
  <verify>
    <automated>cd /Users/vishvah/Documents/GitHub/HoopProphet/hoopprophet && npx vitest run src/__tests__/HitRateChart.test.jsx src/__tests__/PropCard.test.jsx src/__tests__/GameLogTable.test.jsx --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <done>
    - HitRateChart renders 4 bars (L5/L10/L20/Season) with conditional colors and ParentSize responsive wrapper
    - LineSlider renders half-point increment slider with 0.5 step and triggers onLineChange
    - PropCard composes ProbabilityBadge, LineSlider, and HitRateChart into a compact card
    - PropCard grid layout works: 3 cols desktop, 2 tablet, 1 mobile
    - GameLogTable renders compact scrollable table with all stat columns
    - All component tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PlayerHeader, NewsList, HomePage, and PlayerPage</name>
  <files>
    hoopprophet/src/components/PlayerHeader.jsx, hoopprophet/src/components/NewsList.jsx,
    hoopprophet/src/pages/HomePage.jsx, hoopprophet/src/pages/PlayerPage.jsx,
    hoopprophet/src/App.jsx
  </files>
  <action>
    Create the page-level components and player header that compose the full player analysis experience.

    1. **PlayerHeader.jsx** — Per UI-SPEC Component Inventory:
       - Accepts player data: { player, alerts }
       - Layout: flex row with headshot on left, name/position/team + alert badges on right
       - Headshot: NBA CDN URL `https://cdn.nba.com/headshots/nba/latest/1040x760/{player_id}.png`
       - Fallback for missing headshot: initials circle (72px desktop, 48px mobile per UI-SPEC responsive)
       - Player name: text-xl font-semibold text-text-primary
       - Position and team: text-sm text-text-secondary (e.g., "LAL · SF")
       - AlertBadge[] displayed next to player name (max 3, "+N more" for overflow per UI-SPEC)
       - Headshot shrinks to 48px on mobile

    2. **NewsList.jsx** — Per NEWS-03, D-12:
       - Accepts { news, alerts, staleWarning } from useNews hook
       - Renders list of news items with:
         a. AlertBadge for each alert type at top of item
         b. Headline text
         c. Source and "updated X ago" timestamp
         d. Link to original source (if available)
       - Stale warning: if staleWarning is not null, show yellow banner "News data may be outdated. Last updated more than 6 hours ago." per Copywriting Contract
       - Empty state: "No recent news or injury reports for this player." per Copywriting Contract
       - Each item: bg-bg-card, rounded-lg, p-4, border-b border-border

    3. **HomePage.jsx** — Per D-18 and UI-SPEC Page Layout 1:
       - Full viewport height centered layout (min-h-[calc(100vh-80px)])
       - Large HoopProphet branding/logo (text-4xl font-bold text-prob-high)
       - Heading: "Search for a player to get started" (text-xl per Copywriting Contract)
       - Body: "Enter a player name above to see their prop predictions and hit rates."
       - Prominent search input (max-w-md, larger than navbar search)
       - Uses PlayerSearch component from Plan 02
       - On player selection: navigates to /player/{player_id}
       - Error state: "Unable to load data. Check your connection and try again." in red toast
       - Clean, minimal design — no illustrations or pre-populated content per D-18

    4. **PlayerPage.jsx** — Per D-13, D-14, D-16 and UI-SPEC Page Layout 2:
       - Route: /player/:playerId (uses useParams from react-router)
       - Fetches ALL data in parallel on mount using usePlayerData (D-16): player detail, props, alerts
       - Also pre-fetches game logs and news using useGameLogs and useNews
       - Layout structure:
         ```
         <PlayerHeader player={player} alerts={alerts} />
         <TabBar tabs={[{id:'overview', label:'Overview'}, {id:'gamelogs', label:'Game Logs'}, {id:'news', label:'News'}]} activeTab={tab} onTabChange={setTab} />
         {tab === 'overview' && <OverviewTab props={props} playerId={playerId} />}
         {tab === 'gamelogs' && <GameLogsTab gamelogs={gamelogs} />}
         {tab === 'news' && <NewsTab news={news} alerts={alerts} staleWarning={staleWarning} />}
         ```
       - OverviewTab: grid of PropCard components (lg:grid-cols-3 md:grid-cols-2 grid-cols-1)
       - GameLogsTab: GameLogTable component
       - NewsTab: NewsList component
       - Loading state: shows SkeletonCard placeholders (1-5 cards) during data fetch
       - Error state: toast notification per ToastProvider
       - Selection of player from Navbar search updates page via navigation
       - Tab content cross-fade transition (150ms opacity per UI-SPEC)
       - No URL change on tab switch (D-13 note: tabs are UI state, not routes)

    5. **Update App.jsx** — Replace placeholder pages with real component imports:
       ```jsx
       import HomePage from './pages/HomePage';
       import PlayerPage from './pages/PlayerPage';
       // BacktestPage still placeholder for Plan 04
       ```
       - Ensure Navbar component is rendering with PlayerSearch embedded
       - ToastProvider wraps the app
  </action>
  <verify>
    <automated>cd /Users/vishvah/Documents/GitHub/HoopProphet/hoopprophet && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - PlayerHeader renders player name, team/position, headshot, and alert badges
    - NewsList renders news items with alert badges and stale warning
    - HomePage displays search prompt with empty state message
    - PlayerPage renders tabbed layout with Overview/Game Logs/News tabs
    - Overview tab shows PropCard grid (3 cols desktop, 2 tablet, 1 mobile)
    - Game Logs tab shows GameLogTable with scrollable game data
    - News tab shows NewsList with alert badges
    - All data fetched in parallel on PlayerPage mount (usePlayerData with Promise.all)
    - Tab switching uses cross-fade animation (150ms)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → PlayerSearch | Untrusted search input sent to API |
| API → Components | Backend data (player names, stats) rendered in JSX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | Line slider XSS | mitigate | React auto-escapes; line values are numeric from API with server-side validation |
| T-07-09 | Info Disclosure | Game log data | accept | All NBA data is public; no PII in game logs |
| T-07-10 | Denial | Rapid slider changes | mitigate | 300ms debounce on useHitRates prevents API flooding |
</threat_model>

<verification>
1. All tests pass: `npx vitest run`
2. Visx HitRateChart renders 4 bars without console errors
3. LineSlider has step=0.5 for half-point increments
4. PropCard shows ProbabilityBadge, LineSlider, and HitRateChart composed correctly
5. PlayerPage fetches data in parallel (3 simultaneous API calls on mount)
6. HomePage shows empty state message and search input
7. GameLogTable displays with horizontal scroll on narrow viewports
8. NewsList shows stale warning when staleWarning is not null
</verification>

<success_criteria>
- PropCard renders with probability badge, adjustable line slider, and hit rate chart
- Hit rate chart uses Visx v4 alpha with ParentSize for responsive sizing
- Line slider has 0.5 step increments and debounced hit rate refetch
- GameLogTable is compact, scrollable, and handles empty/DNP states
- PlayerPage uses tabbed layout with parallel data fetching
- HomePage shows search landing with empty state
- All component tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend-rebuild/07-03-SUMMARY.md`
</output>