version: "3.9"
services:
  backend:
    build: ./server
    ports:
      - "8000:8000"
    volumes:
      - ./server:/app
  frontend:
    build:
      context: ./hoopprophet
      args:
        - VITE_API_BASE=http://backend:8000
    ports:
      - "3000:3000"
    depends_on:
      - backend